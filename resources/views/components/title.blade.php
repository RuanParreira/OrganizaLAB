<div class="flex flex-col gap-2">
    <div class="flex gap-2 items-center">
        <i class="{{ $icon }} text-{{ $size }} text-violet-500"></i>
        <h2 class="text-3xl font-bold">{{ $title }}</h2>
    </div>
    <p class="text-gray-400">{{ $subtitle }}</p>
</div>
